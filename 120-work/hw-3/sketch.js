

function setup() {
    createCanvas(600, 400);
    background('pink');
}

function draw() {
    //i made my text a little larger
    textSize(60);
    //so I had to drop the y-position some
    text('hello world', 20, 80);

}
